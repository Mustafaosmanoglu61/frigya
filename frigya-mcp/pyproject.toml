[project]
name = "frigya-mcp"
version = "0.1.0"
description = "Frigya portfolio analysis MCP server — wraps frigya-analiz scripts, owns local DB access."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
]

[tool.uv]
package = false
